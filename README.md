Description:
🔧 Automated UI tests for a scooter rental service using Selenium WebDriver + JUnit 4.

Tests FAQ dropdown content visibility

Covers order placement flow (parameterized with 2+ data sets)

Reproduces a known Chrome bug in ordering

Built with Page Object Pattern (Java 11, Maven)

"Tests must pass in at least one browser (Chrome/Firefox). Chrome has a confirmed order submission bug."
